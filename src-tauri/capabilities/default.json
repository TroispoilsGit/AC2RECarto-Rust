{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for AC2RECarto",
  "windows": ["main", "config"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
